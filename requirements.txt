streamlit
SpeechRecognition
```

That's it. Just 2 lines, nothing else.

**And delete `packages.txt`** - we don't need it for this simpler version.

**Your folder should have:**
```
your-project/
├── app.py
└── requirements.txt
